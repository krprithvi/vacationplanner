function updateSlider(slideAmount) {
		alert("inside func");
		console.log("insode the js function");
        var sliderDiv = document.getElementById("sliderAmount");
        sliderDiv.innerHTML = slideAmount;
    }